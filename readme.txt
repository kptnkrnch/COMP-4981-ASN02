Client:
	1. Enter a valid message queue id (ie 7812)
	2. Enter a valid priority level (in the range of 1(low priority) to 10 (high priority))
	-- (note: server must be running by at least this point) --
	3. Enter a valid filename for the server to read and send via the message queue
	
Server:
	1. Enter a valid message queue id (ie 7812)
	2. connect multiple clients by entering the filename to read (client side)